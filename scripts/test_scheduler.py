"""
This script was a temporary test harness used during debugging of the
economic calendar scheduler. It has been intentionally emptied/disabled.

If you need to recreate tests, reintroduce a small harness under
`scripts/` or add pytest unit tests under `tests/`.
"""

if __name__ == '__main__':
    print('test_scheduler.py is deprecated and has no runtime behavior')
